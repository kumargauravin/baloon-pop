{
  "name": "balloon-pop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare-android": "mkdir -p www && cp index.html style.css app.js manifest.json service-worker.js www/ && cp -r icons www/"
  },
  "dependencies": {
    "@capacitor/android": "6.2.0",
    "@capacitor/core": "6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.0"
  }
}
